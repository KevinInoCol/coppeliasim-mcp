# Configuración local: nunca se publica, puede contener rutas personales
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# macOS
.DS_Store

# Artefactos de uv: un lock no pertenece a una libreria, solo a una aplicacion
uv.lock

# Herramientas
# OJO: .mcp.json NO se ignora. Desde que el repositorio es también un plugin de
# Claude Code, ese archivo es lo que declara el servidor MCP del plugin: sin él
# se instala la guía sin las tools, y nada avisa.
.claude/

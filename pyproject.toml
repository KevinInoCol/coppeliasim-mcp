[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coppeliasim-mcp"
version = "0.4.0"
description = "MCP server to drive CoppeliaSim 4.10 from Claude Code and other MCP clients, without exposing arbitrary Lua execution"
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [{ name = "Kevin Inofuente Colque" }]
keywords = ["mcp", "coppeliasim", "robotics", "simulation", "claude", "zmq"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Education",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "mcp>=2.0.0,<3",
    "coppeliasim-zmqremoteapi-client>=2.0.4",
    "python-dotenv>=1.0.0",
    "pyzmq>=25",
]

# PyPI solo enseña estos enlaces en la barra lateral del proyecto, y la
# metadata de una versión ya subida no se puede editar: añadirlos obliga a
# publicar una versión nueva. Sin ellos, quien llega a la página de PyPI no
# tiene forma de encontrar el código ni de abrir un issue.
[project.urls]
Homepage = "https://github.com/KevinInoCol/coppeliasim-mcp"
Repository = "https://github.com/KevinInoCol/coppeliasim-mcp"
Issues = "https://github.com/KevinInoCol/coppeliasim-mcp/issues"
Changelog = "https://github.com/KevinInoCol/coppeliasim-mcp/releases"

[project.scripts]
coppeliasim-mcp = "coppeliasim_mcp.server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/coppeliasim_mcp"]
